class Solution {
public:
    int countCommas(int n) {
        int comma=0;
        string s=to_string(n);
        int m=s.length();
        if(m<4) return 0;
        for(int i=1000;i<=n;i++){
            comma++;
        }
        return comma;
    }
};

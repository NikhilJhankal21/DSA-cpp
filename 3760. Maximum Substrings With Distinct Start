class Solution {
public:
    int maxDistinct(string s) {
        unordered_set<char> seen;
        int count = 0;
        for (char c : s) {
            if (seen.count(c) == 0) {
                count++;
                seen.insert(c);
            }
        }
        return count;
    }
};


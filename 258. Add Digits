class Solution {
public:
    int addDigits(int n) {
        int ans=0;
        while(n>0){
            ans+=n%10;
            n/=10;
        }
        if(ans<10) return ans;
        return addDigits(ans);
    }
};



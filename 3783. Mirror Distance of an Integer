class Solution {
public:
    int mirrorDistance(int n) {
        long long val=n;
        string s=to_string(n);
        reverse(s.begin(),s.end());
        long long rev=stoll(s);
        return abs(val-rev);
    }
};

class Solution {
public:
    int arrayPairSum(vector<int>& nums) {
        int max=INT_MIN;
        int n=nums.size();
        int sum=0;
        sort(nums.rbegin(),nums.rend());
        for(int i=0;i<n;i+=2){
            sum+=min(nums[i],nums[i+1]);
        }
        return sum;
    }
};
